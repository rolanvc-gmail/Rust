pub mod traits; // Declare the 'traits' module whose contents are in the 'traits' folder.
mod base; // Declare the 'base' module whose contents are in the 'base' file.
pub mod done; // Declare the 'done' module whose contents are in the 'done' file.
pub mod pending; // Declare the 'pending' module whose contents are in the 'pending' file.